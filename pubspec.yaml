name: simple_test

description: Ultra simple test suite for Dart scripts using null-safety features.

version: 0.0.2

homepage: https://github.com/simphotonics/simple_test

environment:
  sdk: '>=2.9.1 <3.0.0'

dependencies:
  meta: ^1.2.2

dev_dependencies:
  pedantic: ^1.9.2

dependency_overrides:
  # meta:
  #   git:
  #     url: https://github.com/dart-lang/sdk.git
  #     ref: null_safety-pkgs
  #     path: pkg/meta
